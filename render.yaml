services:
  - type: web
    name: ipt-2026-frontend
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist/ipt-2026-frontend
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
